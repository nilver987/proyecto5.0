export class Category {
    id?: number;                // ID de la categoría, tipo número
    franchise?: string;         // Franquicia a la que pertenece, tipo string (e.g., "Marvel", "DC Comics")
    type?: string;              // Tipo de Funko, tipo string (e.g., "Pop!", "Vinyl Soda")
    popularity?: number;        // Popularidad de la categoría, tipo número
}
